import type { List } from '@/components'

export type RolesAndPermissionList = {
    roles: List
    permissions: List
}

export type CompanionList = {
    TransporterOwners: List
    Drivers: List
}

export type CompaniesList = {
    Companies: List
    CompanyGroups: List
}